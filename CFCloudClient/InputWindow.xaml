<Window x:Class="CFCloudClient.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CFCloudClient"
        mc:Ignorable="d"
        Title="InputWindow" Height="160" Width="360" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>
        <WrapPanel HorizontalAlignment="Left" Height="80" Width="300" Grid.Row="1" Orientation="Vertical" Margin="50, 0, 0, 0">
            <TextBlock x:Name="Input" TextWrapping="Wrap" Text="Input:" Margin="0,0,0,0"/>
            <TextBox x:Name="InputBox" Height="20" Width="250" HorizontalAlignment="Left" Margin="0,10,0,0"/>
            <WrapPanel Margin="0,10,0,0">
                <Button x:Name="Cancel" Content="Cancel" Width="75" Background="White" Margin="0,0,0,0" Click="Cancel_Click"/>
                <Button x:Name="OK" Content="OK" Width="75" Background="White" Margin="100,0,0,0" Click="OK_Click"/>
            </WrapPanel>
        </WrapPanel>
    </Grid>
</Window>
